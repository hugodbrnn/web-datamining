# ─────────────────────────────────────────────────────────────────────────────
# Core dependencies
# ─────────────────────────────────────────────────────────────────────────────

# Web crawling
playwright>=1.40.0

# Information extraction / data handling
requests>=2.31.0

# Named Entity Recognition (NER layer on top of regex extraction)
spacy>=3.7.0
# After install: python -m spacy download en_core_web_sm

# Knowledge Graph — RDF/OWL
rdflib>=7.0.0

# OWL reasoning (optional — enables Pellet in apply_rules.py)
owlready2>=0.46

# ─────────────────────────────────────────────────────────────────────────────
# Knowledge Graph Embeddings
# ─────────────────────────────────────────────────────────────────────────────

# PyTorch (CPU build — for machines without a GPU)
# Install separately:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0

# PyKEEN (TransE, ComplEx, RotatE …)
pykeen>=1.10.0

# ─────────────────────────────────────────────────────────────────────────────
# RAG pipeline + web interface
# ─────────────────────────────────────────────────────────────────────────────

# Local web demo (http://localhost:5000)
flask>=3.0.0

# Ollama Python client (optional — main_rag.py uses urllib directly)
# ollama>=0.1.0

# ─────────────────────────────────────────────────────────────────────────────
# Development / utilities
# ─────────────────────────────────────────────────────────────────────────────
tqdm>=4.66.0
